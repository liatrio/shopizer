apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopizer.fullname" . }}-test-connection"
  labels:
{{ include "shopizer.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "shopizer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
